# Proprietary IRIX sysroot for the cross-toolchain image (never commit)
docker/irix-toolchain/sysroot/
docker/irix-toolchain/sysroot.tgz

# Built EFS/HDD packaging output
dist/

# irixscsitb build outputs (kept out of the tree in case the repo itself is
# shared out over NFS as the build source)
/irixscsitb
/build/
build-*.log
*.tar.gz
*.tar

# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# Host-side mock test binary
tests/irixscsitb-mock

# Build identification headers - generated, never committed.
# version.h is stamped on the host by scripts/mkversion.sh and SHIPPED to the
# build machine as a static input; buildhost.h is written fresh by every build.
version.h
buildhost.h
